# CLAUDE.md — ワークショップ進行ルール

> このファイルは作業フォルダの共通ルールです。各 Level（`01`〜`12`）を進めるとき、Claude は必ず以下の進行ルールに従うこと。`00_START_ここから.md` の指示と本ファイルが競合する場合は、**本ファイルの進行ルールを優先**する。

---

## 開始ルール（最優先）

- ワークショップは **必ず `00_START_ここから.md` から始める**。参加者が個別の Level（例：「Level 4 を試したい」）を指定した場合でも、いきなりその Level ファイルには入らず、まず `00_START_ここから.md` を Read で読み込み、その Phase 0（環境確認）→ Phase 1（レベル選択）の流れに沿って進行する。
- 各 Level ファイル（`01`〜`12`）は、`00_START_ここから.md` の Phase 2 から呼び出されて初めて実行する。

---

## 進行ルール（全 Level 共通・最重要）

各 Level ファイルを Read で読み込んで開始するとき、次の順に進めること。

### A. Level 開始時：概要と Step 一覧を先に提示する

Step の実行に入る前に、まず次の3点を **1回のメッセージ** で示す。

1. **このLevelでできること** — ねらいを2〜3行で。
2. **Step は全部でいくつあるか** — 「全◯ステップ」と明示する。
3. **各 Step の簡単な内容** — 1 Step につき1行の要約リスト。

（Step 一覧は、Read で読み込んだ **Level ファイル本体の見出しを正**として作成する。`Step 4.5` のような枝番があればそれも含める。下の「早見表」はステップ数の目安であり、本体と食い違う場合は本体を優先する。）

提示したら、**「最初の Step から始めます。よろしいですか？」** と確認し、**一旦止まる**。

### B. Step 実行時：1 Step ずつ・都度停止する

- **1 Step ごとに実行 → 出力を止める**。複数 Step を続けて実行しない。
- 各 Step の冒頭で「何のために何をするか」を1行で説明してから実行する。
- Step が終わるごとに必ず次の一文で確認し、**参加者の返事を待ってから次へ進む**：

  > **次の Step に進みますか？**（次は「Step ◯ ◯◯◯」です）

- 「はい／進んで／OK」等の合図があって初めて次の Step に着手する。合図がなければ止まったまま待つ。
- ユーザーが確認や修正を求めている箇所では、勝手に先へ進まない。

### C. Level 終了時

- 最後の Step 完了後、「この Level はここまでです」と伝える。
- **修了記録（スタンプカード）を更新する**: 作業フォルダの `_修了記録.md` に「日付・Level 番号と名前・参加者の一言感想（あれば）」を1行追記する。ファイルが無ければ作成する。追記後、「スタンプが1つたまりました」と軽く伝える。
- 続いて「別の Level も体験しますか？」と尋ね、希望があれば `00_START_ここから.md` の Phase 1（レベル選択）へ戻る。

### 補足

- Level 10 のみ、Step ではなく **Part**（Part 0〜6）構成。呼び方を「Part」に読み替え、同じルール（開始時に一覧提示 → 各 Part 後に「次の Part に進みますか？」で停止）を適用する。
- 分岐がある Step（例：Level 6 の `2-A/2-B`、Level 12 の `2.5` 進路選択）では、分岐の選択を促してから対応する側だけを実行する。
- 出力は日本語・である調を基本に、温かく簡潔に。
- 途中でエラーや接続不良が起きたら、`FAQ_つまずいたら.md` を参照して対処を案内する。
- CLAUDE.md や Memory のしくみについて質問が出たら、`付録_CLAUDEmdとMemoryのしくみ.md` を基に説明する。

---

## 早見表（各 Level の概要とステップ数）

> 各 Step の詳細は各 Level ファイル本体を正とする（二重管理を避けるため、ここには一覧を置かない）。

| Level | ファイル | できること（1行） | 構成 |
|---|---|---|---|
| 1 | `01_はじめての整理整頓.md` | Cowork の基本サイクル「作る→見る→整理を頼む→片付く」を1周体験 | 全4ステップ |
| 2 | `02_Persona設定.md` | 自分専用の設定（Persona）を対話で作り、文章生成に反映 | 全6ステップ |
| 3 | `03_Wordテンプレート差し込み.md` | Word テンプレートをレイアウトを崩さず埋め、PDF 化・Excel 差し込みまで | 全7ステップ |
| 4 | `04_PubMed文献検索.md` | MeSH 検索 → 取得 → EndNote/Word 連携 → 引用検証 | 全7ステップ（4.5含む） |
| 5 | `05_システマティックレビュー.md` | 専用 Skill で文献データ収集〜フォレストプロット | 全6ステップ |
| 6 | `06_スライド生成.md` | デザインを対話で決めてスライド生成 → Skill 化 | 全8ステップ（2-A/2-B分岐） |
| 7 | `07_業務効率化ツール_Chrome拡張.md` | Chrome 拡張（Manifest V3）を作りブラウザに読み込む | 全7ステップ |
| 8 | `08_メールからカレンダー登録.md` | Gmail × カレンダー MCP で未読メール → 予定登録 → Skill 化 | 全7ステップ |
| 9 | `09_定期実行タスク.md` | 朝のブリーフィングを設計 → 試走 → 定期タスク登録 | 全7ステップ |
| 10 | `10_Desktop版Code体験.md` | Claude Desktop「Code」タブを30分で体験 | **全7パート**（Part 0〜6） |
| 11 | `11_画像分類アプリ.md` | 公開教材データで機械学習アプリを学習〜予測まで作る | 全9ステップ（3.5含む） |
| 12 | `12_自動化の3層_Loop_Schedule_Routine.md` | Loop / Schedule / Routine を理解し Schedule を1つ作る | 全9ステップ（2.5/3B含む） |
