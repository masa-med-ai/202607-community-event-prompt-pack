# images — 手順書で使う画像置き場

各モジュール（`0X_*.md`）から参照するスクリーンショット・図版をここに入れる。

Markdown からの参照例:

```markdown
![説明テキスト](./images/ファイル名.png)
```

## 現在参照しているファイル

| ファイル名 | 用途 | 状態 |
|---|---|---|
| `なぜCodeが必要か_CoworkとCodeの違い.png` | レベル9 冒頭のインフォグラフィック。「なぜ Cowork だけでなく Code が要るのか」を5つの違い＋使い分けで1枚絵に（`claude-code-vs-cowork.pptx` 由来） | **配置済み** |
| `自動化の3層_Loop_Schedule_Routine.png` | レベル11 Step1 の比較表。Loop / Schedule / Routine を色分け＋★で1枚絵に（動く場所・条件・トリガー・寿命・許可・選び方） | **配置済み** |
| `discord-claude-channels.png` | （旧 CLI 版で使用）Claude Code Channels の紹介サンプル。現在の Desktop 版手順書では未使用 | 任意 |
| `MCPとは_USB-C解説.html` | レベル7 Step0 冒頭のインフォグラフィック（インタラクティブHTML）。MCP＝Claudeと外部サービスをつなぐ「USB-C（共通差込口）」、コネクタの別名、できること4種、Gmail×カレンダーの例を1枚に。右のサービスをクリックすると各MCPの説明が出る | **配置済み** |

> 配置方法: 該当のスクリーンショットを上記ファイル名でこのフォルダに保存すると、レベル9の手順書内に画像が表示されます。
